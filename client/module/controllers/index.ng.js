angular.module("app").controller("indexCtrl", ['$scope', '$meteor',
  function ($scope, $meteor) {
      console.log('this is loded');
      //TODO set something that indicates the meteor is loaded now
  }]);